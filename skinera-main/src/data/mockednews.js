// Deprecated: demo news data has been removed in favor of live backend APIs.
// Keeping a minimal stub to avoid accidental imports from breaking builds.

export const newsItems = [];

export function getNewsBySlug(slug) {
  return undefined;
}

export function getLatestNews(limit = 4) {
  return [];
}
